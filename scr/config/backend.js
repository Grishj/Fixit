const backend = {
    backendUrl: "http://192.168.10.69:3000",
    // backendUrl: await fetch(`http://localhost:3000/ip`).ip, //No need to enter ip address now, It detects automatically
};

export default backend;
